# 复制本文件为 .env 并填入真实值；.env 已被 .gitignore 排除，严禁提交真实凭据
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# 以下均为可选（已有默认值），按需取消注释覆盖
# LLM_TIMEOUT=120
# MAX_STEPS=20
# MAX_CONTEXT_MESSAGES=40
# LLM_SESSIONS_DIR=~/.litecoder/sessions
